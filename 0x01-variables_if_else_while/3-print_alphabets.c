#include <stdio.h>
/**
 * main - entry point
 *
 * return: always 0 (succes/correct)
 */

int main(viod)

{
char alphabet;
for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
{
if (alphabet == 'q')
contnue;
      
else if (alphabet == 'e')
continue;

putchar(alphabet);
}

putchar('\n');

return (0);

}
